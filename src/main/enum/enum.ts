export enum MessageEnum {
  CLEAR = "Repository cleared!",
  INVALID_ID = "Invalid id!",
}

export enum Status {
  Overdue = "Overdue",
  Completed = "Completed",
  Hold = "Hold",
  Active = "Active",
}
